This project builds a simple 3-layer Neural Network using only NumPy (without Keras or TensorFlow).
- Python
- NumPy
XOR Dataset
1. Install NumPy:
   pip install numpy

2. Run:
   python nn_numpy.py

The model trains using backpropagation and predicts the XOR output.
